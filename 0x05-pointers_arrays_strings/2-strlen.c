#include "main.h"
#include <stdio.h>

/**
 * _strlen - checks the length of a string
 * @s: string to check
 *
 * Return: int length of the string
 */

int _strlen(char *s)
{
	int length = 0;

	length = _strlen(s);
	printf("%d\n", length);
	return (0);
}
